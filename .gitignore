# Dependencias de Node.js
# Esta carpeta se regenera con `npm install` y no debe estar en el repositorio.
/node_modules

# Archivos generados por el proceso de build.
# Es una buena práctica generarlos y no guardarlos en el control de versiones.
/css/bundle.min.css
/js/bundle.min.js

# Archivos del sistema operativo
.DS_Store